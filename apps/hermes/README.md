# Hermes Runtime Scaffold

This folder provides Docker runtime configuration for Hermes.

- Runtime is managed by `docker-compose.yml`.
- Config lives in `hermes.config.yaml`.
- Persistent runtime data is mounted to `/app/data`.
