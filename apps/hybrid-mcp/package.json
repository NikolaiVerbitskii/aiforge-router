{
  "name": "@apps/hybrid-mcp",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "lint": "eslint .",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/express": "5.0.6",
    "@types/node": "25.6.0",
    "@types/supertest": "7.2.0",
    "eslint": "catalog:lint",
    "supertest": "7.2.2",
    "tsx": "4.21.0",
    "typescript": "catalog:",
    "vitest": "catalog:test"
  },
  "dependencies": {
    "express": "5.2.1"
  }
}
